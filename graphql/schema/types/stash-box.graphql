type StashBox {
    endpoint: String!
    api_key: String!
}

input StashBoxInput {
    endpoint: String!
    api_key: String!
}

type SceneStashID {
  scene_id: ID!
  endpoint: String!
  stash_id: String!
}

type PerformerStashID {
  performer_id: ID!
  endpoint: String!
  stash_id: String!
}

type StudioStashID {
  studio_id: ID!
  endpoint: String!
  stash_id: String!
}

input StashIDInput {
  endpoint: String!
  stash_id: String!
}
